name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 9 */3 * *'  # Se ejecuta cada 3 días a las 9 AM UTC
  workflow_dispatch:         # También permite ejecutarlo manualmente desde GitHub

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping a Supabase
        run: |
          curl -s "https://acvjodjnjjwujrkriztc.supabase.co/rest/v1/resenas?select=id&limit=1" \
            -H "apikey: sb_publishable__wLe7LUdE85jiO7UY8zQuA_tWCa76-l" \
            -H "Authorization: Bearer sb_publishable__wLe7LUdE85jiO7UY8zQuA_tWCa76-l"
          echo "✅ Ping enviado a Supabase correctamente"
